version: '3.5'
services:
  mysingboxconverter:
    build: .
    image: mysingboxconverter
    ports:
      - "5000:5000"
    networks:
      - subconverter

networks:
  subconverter:
    name: subconverter
    external: true
