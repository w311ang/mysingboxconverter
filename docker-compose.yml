version: '3.5'
services:
  mysingboxconverter:
    build: .
    image: mysingboxconverter
    ports:
      - "localhost:45288:5000"
    networks:
      - subconverter

networks:
  subconverter:
    name: subconverter
    external: true
